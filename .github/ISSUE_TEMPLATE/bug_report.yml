name: Bug report
description: Something is broken or wrong (install, triggers, overlay, sound, TTS).
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting a bug. For feature ideas, use [Discussions → Ideas](https://github.com/kpxcoolx/eql-alerts/discussions/categories/ideas) instead.
  - type: dropdown
    id: platform
    attributes:
      label: Platform
      options:
        - Windows (installer)
        - Windows (from source)
        - Mac + Parallels
        - Other
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: App version
      placeholder: e.g. v0.1.1
    validations:
      required: false
  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: Steps to reproduce and what you expected.
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: Relevant log lines (optional)
      description: Paste a few matching lines if a trigger missed or misfired. Redact anything personal.
      render: text
    validations:
      required: false
