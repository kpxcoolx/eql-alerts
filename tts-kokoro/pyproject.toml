[project]
name = "eql-kokoro"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "kokoro-onnx>=0.5.0",
    "numpy>=2.5.1",
    "soundfile>=0.14.0",
]
